module uitool.ui.comkey {
    import Sprite = sear.Sprite;

    /**
     * 组件的一条属性
     * 
     * @author pbk
     */
    export class KeyItem extends Sprite {
        
        constructor() {
            super();
        }

        clear(): void {
        }
    }
}
module sear.maths {

    /**
     * 矩形
     * 
     * @author pbk
     */
    export class Rectangle extends laya.maths.Rectangle {
        constructor(x: number = 0, y: number = 0, width: number = 0, height: number = 0) {
            super(x, y, width, height);
        }
    }
}